[package]
name = "btcser-mutator"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", features = ["small_rng"] }
btcser = { path = "../btcser" }

[lib]
name = "btcser_mutator"
crate-type = ["cdylib", "staticlib", "rlib"]
